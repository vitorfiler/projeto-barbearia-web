import { ValueTimePipe } from './value-time.pipe';

describe('ValueTimePipe', () => {
  it('create an instance', () => {
    const pipe = new ValueTimePipe();
    expect(pipe).toBeTruthy();
  });
});
